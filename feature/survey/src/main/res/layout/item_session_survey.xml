<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    >

    <data>

        <import type="android.view.View"/>

        <variable
            name="surveyItem"
            type="io.github.droidkaigi.confsched2019.survey.ui.widget.SurveyItem"
            />
    </data>

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center"
        >

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="@{surveyItem.title(context)}"
            android:textSize="24sp"
            tools:text="@string/as_expected"
            />

        <androidx.appcompat.widget.AppCompatRatingBar
            android:id="@+id/rating"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:layout_marginTop="40dp"
            android:numStars="5"
            android:stepSize="1"
            android:visibility="@{surveyItem.isComment ? View.GONE : View.VISIBLE}"
            />

        <androidx.appcompat.widget.AppCompatEditText
            android:id="@+id/comment"
            android:layout_width="match_parent"
            android:layout_height="44dp"
            android:gravity="center_vertical"
            android:hint="@string/comment_hint"
            android:paddingEnd="10dp"
            android:paddingStart="10dp"
            android:visibility="@{surveyItem.isComment ? View.VISIBLE : View.GONE}"
            tools:visibility="visible"
            />

    </LinearLayout>
</layout>
